# Secrets — never commit
.env
google-play-service-account.json
*.keystore
*.jks

# Expo / build artifacts
.expo/
dist/
.expo-export-test/
web-build/

# native (managed workflow regenerates these)
/ios
/android

# local scratch payloads (API request bodies, etc.)
scratch_payload.json

# App Store Connect API key (never commit)
AuthKey_*.p8

# EAS signing credentials (keystore passwords) — never commit.
credentials.json
