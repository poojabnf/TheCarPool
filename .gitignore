# Dependencies and package managers
node_modules/
venv/
.pnpm-store/
package-lock.json.log
yarn-error.log

# Build outputs
dist/
build/
.next/
.expo/
out/
web-build/

# Local configuration and environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# IDEs and Editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS metadata files
.DS_Store
Thumbs.db

# Graphify data & report outputs
.graphify/
graphify-out/
*.sqlite
*.sqlite-shm
*.sqlite-wal
screenshots/

# Firebase service-account key — a signing credential, never committed.
firebase-adminsdk*.json
.vercel

# EAS tooling regenerates a stub app.json at the repo root. Any eas command
# run from here reads it INSTEAD of thecarpool-mobile/app.json, which has
# silently pointed builds at the wrong project. The real config lives in
# thecarpool-mobile/.
/app.json
